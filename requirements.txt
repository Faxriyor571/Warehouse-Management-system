# ============================================================
# Warehouse Management System - Python dependencies
# Python 3.13+
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Templating ---
jinja2==3.1.5

# --- Database / ORM / Migration ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Settings / Validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Security / Auth ---
# bcrypt is used directly (no passlib) for Python 3.13 compatibility:
# passlib 1.7.4 breaks on 3.13 (removed 'crypt' stdlib) and with bcrypt>=4.1.
bcrypt==4.2.1
pyjwt==2.10.1

# --- Reports / Export ---
openpyxl==3.1.5
reportlab==4.2.5

# --- Utilities ---
python-dotenv==1.0.1

# --- Testing (dev) ---
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
